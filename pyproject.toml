[project]
name = "ansible-collection-boilerplate"
version = "2.0.0"
description = "Ansible Boilerplate Collection"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible-core>=2.18.6",
    "ansible-lint>=25",
]
